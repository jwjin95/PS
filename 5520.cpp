#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

#define MAXN 10

int clock[MAXN];


int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    for(int i=1 ; i<=9 ; i++) cin >> clock[i];

}